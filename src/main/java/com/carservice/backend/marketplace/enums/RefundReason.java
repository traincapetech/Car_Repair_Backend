package com.carservice.backend.marketplace.enums;

public enum RefundReason {
    OPPORTUNITY_ALREADY_ASSIGNED,
    CUSTOMER_CANCELLED,
    SERVICE_UNAVAILABLE,
    DUPLICATE_PAYMENT,
    MANUAL_ADMIN_REFUND,
    OTHER
}
