spring.application.name=backend

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/car_service_dev}
spring.datasource.username=${DB_USERNAME:car_service_user}
spring.datasource.password=${DB_PASSWORD:Canada@1212}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_FORMAT_SQL:false}
spring.jpa.open-in-view=false

# JWT Configuration
jwt.secret=${JWT_SECRET:yc8t2YLPbW1HTwLWHoFG2mpaCbPGZsHsoU9uV079wnw=}

# 15 minutes (in milliseconds)
jwt.access-token-expiration=${JWT_ACCESS_TOKEN_EXPIRATION:900000}

# 7 days (in milliseconds)
jwt.refresh-token-expiration=${JWT_REFRESH_TOKEN_EXPIRATION:604800000}

server.port=${PORT:8080}

password-reset.token-expiration=${PASSWORD_RESET_TOKEN_EXPIRATION:15}